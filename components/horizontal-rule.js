import styled from 'styled-components';

import * as colors from '../constants/colors';

const HorizontalRule = styled.hr`
  background-color: 1px solid ${colors.darkGray};
  border: 0;
  height: 1px;
  width: 100%;
`;

export default HorizontalRule;
